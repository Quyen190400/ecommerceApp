spring:
  application:
    name: ecommerceApp
  datasource:
    url: jdbc:sqlserver://localhost:1433;DatabaseName=GreenteaShop;encrypt=true;trustServerCertificate=true;createDatabaseIfNotExist=true;characterEncoding=UTF-8;useUnicode=true
    username: nthquyen
    password: nthquyen #Quyen190400!
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  thymeleaf:
    cache: false
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.SQLServerDialect
        format_sql: true
        connection:
          characterEncoding: UTF-8
          useUnicode: true

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

